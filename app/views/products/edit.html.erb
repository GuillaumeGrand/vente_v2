<h1>Products#edit</h1>
<%= simple_form_for [@store, @product], :url => product_path, method: :patch do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>

  <%= f.input :presentation %>

  <%= f.input :stock %>

  <%= f.input :price %>

  <%= f.button :submit, class: "btn-primary" %>
  <%= f.button :button, "Cancel", type: "reset", class: "btn-outline-secondary" %>
<% end %>
