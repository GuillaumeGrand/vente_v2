<% @orders.each_with_index do |key, value| %>
    <div class="order_unit">
        <div class="order_name">
          <h4><%= key[1][0].user.email %></h4>
          <h4><%= "total :#{@amount[value]}" %></h4>
          <%= link_to 'commande terminé', validation_order_path(:cart => value), method: :post, remote: true %>
        </div>
          <% key[1].each do |order| %>
            <div class="card-order">
              <div class="card-order-infos" id= "order_info">
                <h4><%= order.product.name %></h3>
                <h3><%= "quantiter : #{order.quantity}" %></p>
                <p><%= "prix unitaire : #{order.product.price_cents / 100}"%></p>
              </div>
            </div>
          <% end %>
    </div>
<% end %>

